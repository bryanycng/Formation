<p>
  <strong>User:</strong>
  <%= @user.name %>
</p>
 
<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<%= image_tag @user.avatar.url(:medium) %> 
<br>

<% if logged_in? %>
<%= link_to 'edit', edit_user_path(@user) %>
<% end %>
<%= link_to 'Back', users_path %>